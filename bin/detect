#!/bin/bash

BUILD_DIR=$1

if [ -f $BUILD_DIR/.bitwarden-web-version ]; then
	WEB_VAULT_VERSION="$(cat $BUILD_DIR/.bitwarden-web-version)"
	echo "--> Using bitwarden web-vault $WEB_VAULT_VERSION"
	exit 0
fi

exit 1
